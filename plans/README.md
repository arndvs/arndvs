# plans/

Formal PRDs and issue breakdowns created by `/write-a-prd` and `/prd-to-issues`.

Plans are distinct from active execution files in `working/active/`. A PRD defines _what_ to build; an active plan tracks _where you are_ in building it.

**Lifecycle:** Archive or delete after work completes.
